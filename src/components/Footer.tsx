import { Box } from '@mui/material';

const Footer = () => {
  return (
    <Box
      component='footer'
      sx={{ mt: 'auto', bgcolor: 'secondary.dark', color: 'text' }}
    >
      This is a footer
    </Box>
  );
};

export default Footer;
